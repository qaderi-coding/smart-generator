const BACKEND_DIR_NAME = "{{backendDirName}}"
const CONTRACT_NAME = "{{contractName}}"
const NETWORK = "{{network}}"
const FRONTEND_DIR_NAME = "{{frontendDirName}}"

module.exports = {
    BACKEND_DIR_NAME,
    CONTRACT_NAME,
    NETWORK,
    FRONTEND_DIR_NAME
}